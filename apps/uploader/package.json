{
  "name": "amrss-uploader",
  "version": "0.1.0",
  "private": true,
  "description": "AMRSS offline de-identification and upload client for WHONET laboratories",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').cpSync('src/renderer','dist/renderer',{recursive:true,filter:p=>!p.endsWith('.ts')})\"",
    "dev": "npm run build && electron .",
    "test": "npm run build && node --test 'dist/test/**/*.test.js'",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "better-sqlite3": "11.8.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.12",
    "@types/node": "22.10.7",
    "electron": "33.3.1",
    "typescript": "5.7.3"
  }
}
